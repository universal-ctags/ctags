/*
 * Generated by ./misc/optlib2c from optlib/prolog.ctags, Don't edit this manually.
 */
#include "general.h"
#include "parse.h"
#include "routines.h"
#include "field.h"
#include "xtag.h"
#include "selectors.h"


static void initializePrologParser (const langType language)
{

	addLanguageRegexTable (language, "main");
	addLanguageRegexTable (language, "args");
	addLanguageRegexTable (language, "impl");
	addLanguageRegexTable (language, "comment");
	addLanguageRegexTable (language, "comment_multiline");
	addLanguageRegexTable (language, "comment_oneline");
	addLanguageRegexTable (language, "any");
	addLanguageRegexTable (language, "ignoreWhiteSpace");
	addLanguageRegexTable (language, "skipstr1");
	addLanguageRegexTable (language, "skipstr2");
	addLanguageRegexTable (language, "strextract");
	addLanguageRegexTable (language, "skipparen");

	addLanguageTagMultiTableRegex (language, "main",
	                               "^/\\*",
	                               "", "", "{tenter=comment_multiline}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^\\%",
	                               "", "", "{tenter=comment_oneline}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^[ \t\n]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^([a-zA-Z_][:a-zA-Z_0-9]*)",
	                               "\\1", "p", "{scope=push}"
		"{{\n"
		"   _scopedepth 1 gt {\n"
		"      _scopepop\n"
		"      . _markplaceholder\n"
		"   } if\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^'",
	                               "", "", "{tenter=strextract}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^(\\()",
	                               "", "", "{tenter=args}"
		"{{\n"
		"   % For counting arity\n"
		"   0\n"
		"   % pstack (----) = _scopetop { dup :line =+ (/) =+ @1 _matchloc2line = :name = } if (====) =\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^\\.",
	                               "", "", "{scope=pop}"
		"{{\n"
		"   _scopetop {\n"
		"      % If a clause has no head, no arity value is on the stack.\n"
		"      count 2 ge {\n"
		"         % arity:int arity:int predicate:tag\n"
		"         exch Prolog.arity:\n"
		"      } {\n"
		"         pop\n"
		"      } ifelse\n"
		"   } if\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^:-",
	                               "", "", "{tenter=impl}", NULL);
	addLanguageTagMultiTableRegex (language, "main",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^/\\*",
	                               "", "", "{tenter=comment_multiline}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^\\%",
	                               "", "", "{tenter=comment_oneline}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^[ \t\n]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^0'.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^'",
	                               "", "", "{tenter=skipstr1}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^\"",
	                               "", "", "{tenter=skipstr2}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^\\)",
	                               "", "", "{tleave}"
		"{{\n"
		"  /Prolog.arityAppended _extraenabled {\n"
		"       % arity:int arity:int\n"
		"     dup 0 string cvs\n"
		"     % arity:int arity:string\n"
		"     _scopetop {\n"
		"        % arity:int arity:string predicate:tag\n"
		"        dup :name\n"
		"        % arity:int arity:string predicate:tag name:string\n"
		"        3 -1 roll\n"
		"        % arity:int predicate:tag name:string arity:string\n"
		"        [ 3 1 roll\n"
		"        % arity:int predicate:tag [ name:string arity:string\n"
		"        (/) exch\n"
		"        % arity:int predicate:tag [ name:string (/) arity:string\n"
		"        _buildstring exch\n"
		"        % arity:int name-wit-arity:string predicate:tag\n"
		"        dup :kind exch\n"
		"        % arity:int name-wit-arity:string kind predicate:tag\n"
		"        _tagloc\n"
		"        % arity:int name-wit-arity:string kind loc\n"
		"        _tag _commit /Prolog.arityAppended _markextra\n"
		"        % arity:int\n"
		"     } if\n"
		"  } if\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^[a-z]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^,",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^([A-Z][A-Za-z]*)",
	                               "\\1", "v", "{scope=ref}"
		"{{\n"
		"   1 add\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "args",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^/\\*",
	                               "", "", "{tenter=comment_multiline}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^\\%",
	                               "", "", "{tenter=comment_oneline}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^[ \t\n]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^0'.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^'",
	                               "", "", "{tenter=skipstr1}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^\"",
	                               "", "", "{tenter=skipstr2}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^\\(",
	                               "", "", "{tenter=skipparen}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^=\\.\\.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^[0-9]+\\.[0-9]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^\\.",
	                               "", "", "{tleave}{_advanceTo=0start}", NULL);
	addLanguageTagMultiTableRegex (language, "impl",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "comment",
	                               "^/\\*",
	                               "", "", "{tenter=comment_multiline}", NULL);
	addLanguageTagMultiTableRegex (language, "comment",
	                               "^\\%",
	                               "", "", "{tenter=comment_oneline}", NULL);
	addLanguageTagMultiTableRegex (language, "comment_multiline",
	                               "^\\*/",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "comment_multiline",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "comment_oneline",
	                               "^[\n]",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "comment_oneline",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "any",
	                               "^.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "ignoreWhiteSpace",
	                               "^[ \t\n]+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "skipstr1",
	                               "^[^']*'",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "skipstr1",
	                               "^",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "skipstr2",
	                               "^[^\"]*\"",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "skipstr2",
	                               "^",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "strextract",
	                               "^([^']*')",
	                               "'\\1", "p", "{scope=push}{tleave}"
		"{{\n"
		"   _scopedepth 1 gt {\n"
		"      _scopepop\n"
		"      . _markplaceholder\n"
		"   } if\n"
		"}}", NULL);
	addLanguageTagMultiTableRegex (language, "strextract",
	                               "^",
	                               "", "", "{tleave}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^[^%\\(\\)/0]'\"+",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^/\\*",
	                               "", "", "{tenter=comment_multiline}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^\\%",
	                               "", "", "{tenter=comment_oneline}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^0'.",
	                               "", "", "", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^'",
	                               "", "", "{tenter=skipstr1}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^\"",
	                               "", "", "{tenter=skipstr2}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^\\(",
	                               "", "", "{tenter=skipparen}", NULL);
	addLanguageTagMultiTableRegex (language, "skipparen",
	                               "^\\)",
	                               "", "", "{tleave}", NULL);
}

extern parserDefinition* PrologParser (void)
{
	static const char *const extensions [] = {
		"pl",
		NULL
	};

	static const char *const aliases [] = {
		"swipl",
		"gprolog",
		"yap",
		"xsb",
		NULL
	};

	static const char *const patterns [] = {
		NULL
	};

	static kindDefinition PrologKindTable [] = {
		{
		  true, 'p', "predicate", "predicates",
		},
		{
		  false, 'z', "variable", "variables",
		},
	};
	static fieldDefinition PrologFieldTable [] = {
		{
		  .enabled     = true,
		  .name        = "arity",
		  .description = "arity of predicate",
		  .dataType = FIELDTYPE_SCRIPTABLE|FIELDTYPE_INTEGER,
		  .isValueAvailable = isValueAvailableGeneric,
		  .getValueObject = getFieldValueGeneric,
		  .setValueObject = setFieldValueGeneric,
		},
	};
	static xtagDefinition PrologXtagTable [] = {
		{
		  .enabled     = true,
		  .name        = "arityAppended",
		  .description = "Include predicates with their arises",
		},
	};
	static selectLanguage selectors[] = { selectPerlOrPrologByDistinctiveToken, NULL };

	parserDefinition* const def = parserNew ("Prolog");

	def->versionCurrent= 0;
	def->versionAge    = 0;
	def->enabled       = true;
	def->extensions    = extensions;
	def->patterns      = patterns;
	def->aliases       = aliases;
	def->selectLanguage= selectors;
	def->method        = METHOD_NOT_CRAFTED|METHOD_REGEX;
	def->useCork       = CORK_QUEUE;
	def->kindTable     = PrologKindTable;
	def->kindCount     = ARRAY_SIZE(PrologKindTable);
	def->fieldTable    = PrologFieldTable;
	def->fieldCount    = ARRAY_SIZE(PrologFieldTable);
	def->xtagTable     = PrologXtagTable;
	def->xtagCount     = ARRAY_SIZE(PrologXtagTable);
	def->initialize    = initializePrologParser;

	return def;
}
